import { CreateListingForm } from '@/components/listings/listings.form';

const HomePage = () => {
  return (
    <section>
      <h1>Hello WORLD</h1>
      <CreateListingForm />
    </section>
  );
};

export default HomePage;
