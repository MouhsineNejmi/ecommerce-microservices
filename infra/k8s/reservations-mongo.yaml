apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: reservations-mongo-pvc
spec:
  resources:
    requests:
      storage: 100Mi
  accessModes:
    - ReadWriteOnce
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reservations-mongo-depl
spec:
  selector:
    matchLabels:
      app: reservations-mongo
  template:
    metadata:
      labels:
        app: reservations-mongo
    spec:
      containers:
        - name: reservations-mongo
          image: mongo
          volumeMounts:
            - name: reservations-mongo-storage
              mountPath: '/data/db'
      volumes:
        - name: reservations-mongo-storage
          persistentVolumeClaim:
            claimName: reservations-mongo-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: reservations-mongo-srv
spec:
  selector:
    app: reservations-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
